bucket_definitions:
  global:
    data:
      # Sync all authors
      - SELECT * FROM authors

      # Sync all collections
      - SELECT * FROM collections

      # Sync all quotes
      - SELECT * FROM quotes

      # Sync all editors
      - SELECT * FROM editors
